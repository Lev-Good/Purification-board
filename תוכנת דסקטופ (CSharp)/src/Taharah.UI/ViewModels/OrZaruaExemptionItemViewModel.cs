namespace Taharah.UI.ViewModels;

/// <summary>One Or Zarua exemption line for the day-state panel - pre-formatted text, since the panel only ever displays it, never edits it.</summary>
public sealed class OrZaruaExemptionItemViewModel
{
    public string Text { get; set; } = string.Empty;
}
