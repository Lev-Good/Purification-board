# TASKS.md

## הושלם (סבב 2026-09-17 — סריקה והכשרה להפצה)

- [x] סריקת כל קבצי הפרויקט (HTML/CSS/JS/Electron) והבנת הארכיטקטורה המלאה.
- [x] בדיקה חיה בדפדפן (שרת סטטי מקומי) של כל הזרימות: הגדרת PIN, נעילה/פתיחה, קוד שגוי, שכחתי-קוד, הוספת ראייה, הפסק טהרה עם אזהרת 5 ימים, שבעה נקיים, צפי טבילה, תצוגה שנתית, מודל מייל, גיבוי, עדכון PIN, הגדרת אור זרוע — ללא שגיאות קונסול.
- [x] איתור ותיקון SVG שבור (אייקון "ריכוז וסתות") בשלושה מקומות.
- [x] איתור ותיקון משתני CSS לא מוגדרים (`--shadow-lg`, `--surface-hover`).
- [x] איתור ותיקון קובץ `icon.png` חסר מקונפיגורציית אריזת Electron.
- [x] הסרת קוד מת (stub functions לא בשימוש).
- [x] אימות מלא של תהליך האריזה: `npm install`, `npm run pack`, הרצת קובץ ה-exe שנוצר ווידוא פתיחה תקינה עם אייקון וכותרת נכונים.
- [x] כתיבת תיעוד פרויקט מלא (`docs/SPEC.md`, `docs/ARCHITECTURE.md`, `docs/DECISIONS.md`, `docs/AGENT_LOG.md`, `docs/CHANGELOG.md`).

## הושלם (סבב 2026-09-17 המשך — מימוש "אפיון תוספות עתידיות מתוכננות")

מימוש מלא של כל הסעיפים ברשימת התוספות העתידיות שהמשתמש ציין (`אפיון תוספות עתידיות מתוכננות.txt`, קובץ מקומי לא במעקב git):

- [x] כפתורי זום גלובליים (70%-150%, בסרגל הצד ובתפריט המובייל).
- [x] יום הלכתי לפי שקיעה: בחירת עיר בהגדרות (`js/zmanim.js`), משפיע על "היום" בכל האפליקציה.
- [x] תיקון מיקום סימוני "נקיים" (יום 1) ו"טבילה" בלוח לפי הכלל של יום עברי המתחיל בלילה — לפי הדוגמה המדויקת שנתן המשתמש (הפסק בט"ו → נקיים יום 1 בראש ט"ז, טבילה בראש כ"ג).
- [x] חישוב תאריך לידה משוער (ראייה אחרונה + 280 יום), מוצג רק כאשר עברו 35+ יום ללא הפסק, עם אזהרה מפורשת שזו הערכה בלבד.
- [x] בדיקת עדכון גרסה אוטומטית מול GitHub Releases + כפתור הורדה/התקנה + גיבוי בטיחות אוטומטי לפני התקנה.
- [x] החלפת כל האימוג'ים באייקוני SVG בכל האפליקציה (כולל תיקון באג שבו אייקון ה-SVG המקורי של כפתור העיצוב נדרס בפועל ע"י אימוג'י ב-`updateThemeIcon`).
- [x] כניסה אוטומטית לאחר הקלדת הספרה השישית של קוד הגישה.
- [x] התראות מחשב/דפדפן (כבוי/יומי/ימי אירוע בלבד).
- [x] עיצוב מינימליסטי: צבעי מרקר שטוחים (לא גרדיאנט), הסרת אנימציית פעימה על "היום", תיקון `.alert-pulse` שהיה חסר לגמרי מה-CSS.
- [x] הכנה סופית להפצה: תיקון `icon.png` שהיה בפועל JPEG, הוספת `LICENSE` (GPL-2.0-or-later, טקסט רשמי), כתיבת `README.md` אמיתי, אריזת `npm run dist` מלאה ואימות הרצת קובץ ה-exe.

## ממתין (לא בוצע — לשיקול המשתמש)

- [ ] החלטה האם לשנות את מנגנון שחזור ה-PIN (כרגע נשלח בטקסט גלוי לשירות Google Apps Script חיצוני) לפתרון מאובטח יותר (למשל OTP חד-פעמי).
- [ ] תמיכה בבנייה ל-macOS/Linux (אם רלוונטי להפצה עתידית — כרגע Windows בלבד).
- [ ] הצפנת קובצי הגיבוי (JSON) — כרגע נשמרים כטקסט גלוי.
- [ ] בדיקת `npm audit` (חולשות זוהו בתלויות הפיתוח של electron-builder, לרוב ב-devDependencies שאינן משפיעות על האפליקציה הארוזה עצמה) — לא טופל, לא התבקש.
- [ ] בדיקת QA ידנית של אינטראקציות native ב-Windows (לחיצה על אייקון במגש המערכת, תפריט המגש) — נבדק לוגית ובאמצעות הרצת התוכנה הארוזה בפועל (עלתה ללא קריסה, כותרת חלון נכונה), אך לא בוצעה לחיצה ידנית על אייקון המגש עצמו (מחוץ להיקף כלי האוטומציה הזמינים בסשן זה).
- [ ] פרסום גרסה בפועל ל-GitHub Releases (`npm run dist -- --publish always` עם `GH_TOKEN`) — הקונפיגורציה מוכנה אך לא בוצע פרסום, בהתאם למדיניות הבטיחות (פעולה בעלת נראות ציבורית הדורשת אישור מפורש).
