# TASKS.md

## הושלם (סבב 2026-09-17 — סריקה והכשרה להפצה)

- [x] סריקת כל קבצי הפרויקט (HTML/CSS/JS/Electron) והבנת הארכיטקטורה המלאה.
- [x] בדיקה חיה בדפדפן (שרת סטטי מקומי) של כל הזרימות: הגדרת PIN, נעילה/פתיחה, קוד שגוי, שכחתי-קוד, הוספת ראייה, הפסק טהרה עם אזהרת 5 ימים, שבעה נקיים, צפי טבילה, תצוגה שנתית, מודל מייל, גיבוי, עדכון PIN, הגדרת אור זרוע — ללא שגיאות קונסול.
- [x] איתור ותיקון SVG שבור (אייקון "ריכוז וסתות") בשלושה מקומות.
- [x] איתור ותיקון משתני CSS לא מוגדרים (`--shadow-lg`, `--surface-hover`).
- [x] איתור ותיקון קובץ `icon.png` חסר מקונפיגורציית אריזת Electron.
- [x] הסרת קוד מת (stub functions לא בשימוש).
- [x] אימות מלא של תהליך האריזה: `npm install`, `npm run pack`, הרצת קובץ ה-exe שנוצר ווידוא פתיחה תקינה עם אייקון וכותרת נכונים.
- [x] כתיבת תיעוד פרויקט מלא (`docs/SPEC.md`, `docs/ARCHITECTURE.md`, `docs/DECISIONS.md`, `docs/AGENT_LOG.md`, `docs/CHANGELOG.md`).

## ממתין (לא בוצע בסבב זה — לשיקול המשתמש)

- [ ] החלטה האם לשנות את מנגנון שחזור ה-PIN (כרגע נשלח בטקסט גלוי לשירות Google Apps Script חיצוני) לפתרון מאובטח יותר (למשל OTP חד-פעמי).
- [ ] תמיכה בבנייה ל-macOS/Linux (אם רלוונטי להפצה עתידית — כרגע Windows בלבד).
- [ ] הצפנת קובצי הגיבוי (JSON) — כרגע נשמרים כטקסט גלוי.
- [ ] בדיקת `npm audit` (10 חולשות זוהו בתלויות הפיתוח של electron-builder, לרוב ב-devDependencies שאינן משפיעות על האפליקציה הארוזה עצמה) — לא טופל בסבב זה, לא התבקש.
