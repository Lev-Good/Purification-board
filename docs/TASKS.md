# משימות

מסמך זה מרכז את המשימות המשמעותיות של הפרויקט.
עודכן לאחרונה: 2026-09-22.

> **האפיון ההלכתי המלא:** `docs/SPEC_DINIM_VESATOT.md` — מבוסס על פענוח ספר
> "שיעורי טהרה" (הגרמ"ג, דיני וסתות), עם מראי מקום לעמוד ולשיעור. הפערים
> בסעיפים א'–ו' להלן מופו מחדש ומקוטלגים שם (§7) לפי חומרה.
>
> **חלון ביוץ ופוריות ומסך תובנות:** `docs/SPEC_FERTILITY_INSIGHTS.md` — תכונה
> נפרדת (לא הלכתית-וסתות), כבויה כברירת מחדל. מומשה במלואה (2026-09-22) —
> ראו הרשומה תחת "הושלם" להלן.

---

## הושלם

- [x] **חלון ביוץ ופוריות ומסך תובנות (2026-09-22):** `docs/SPEC_FERTILITY_INSIGHTS.md`
      מומש במלואו — `js/fertility.js` (יום ביוץ משוער, חלון פוריות, זיהוי "עקרות
      הלכתית") ו-`js/insights.js` (סטטיסטיקת מחזורים, גרף SVG, דוח הדפסה), שניהם
      כבויים/עצמאיים מהמנוע ההלכתי. חיבור ל-`js/app.js` (`computeEngineData()`),
      סימוני לוח וכרטיס דשבורד ומסך "מדדים ותובנות" מלא ב-`js/ui.js`, מקטע הגדרות
      "פוריות וביוץ" (סגור וכבוי כברירת מחדל) ב-`index.html`. כיבוי אוטומטי בהריון
      פתוח ובנטילת כדורים, ארבע הופעות של ההבהרה הרפואית/הלכתית. `npm test` עבר
      במלואו (21/21 קבצים, 777 בדיקות) כולל `tests/fertility.test.js` ו-
      `tests/insights.test.js` החדשים, ואומת ידנית בדפדפן (שרת סטטי מקומי): empty
      state, סימוני לוח (חלון + שיא), כרטיס דשבורד עם התרעת עקרות הלכתית, מסך
      תובנות מלא עם נתונים אמיתיים, טופס ההגדרות, כיבוי אוטומטי בהריון, ודוח הדפסה.
      ראו `docs/DECISIONS.md` (2026-09-22) להכרעות שהתקבלו בדרך.
- [x] **נגישות: מקלדת, ניגודיות, זום, לא-מקוון (2026-09-18, סבב שלישי):** משבצות
      הלוח נגישות למקלדת (role/tabindex/aria-label + הפעלת Enter/Space), Escape
      סוגר מודאלים (מחסנית פתיחה אמיתית, לא סדר ה-DOM), טוסטים מוכרזים
      (`aria-live`), זום הוחזר במובייל, ניגודיות תגיות הסימון ("700"→"800" במקום
      "500"→"600", נמדד עובר 5.0–6.7:1 לעומת 2.1–3.8:1 קודם), ו-Service Worker
      חדש (`sw.js`) שנותן תוקף אמיתי ל-manifest ה-PWA (עבודה בלי רשת) — רק בגרסת
      האתר. `npm test` עבר במלואו (731), ואומת ידנית כולל מצב לא-מקוון בפועל.
      ראו `docs/AGENT_LOG.md` ו-`docs/DECISIONS.md`.
- [x] **הערה מלאה בכרטיסון + קיפול מתגי חומרא (2026-09-18, סבב שני):** אייקון ההערה
      בלוח (חודשי/שנתי) פותח כעת כרטיסון עם הטקסט המלא (`showDayNotePreview`,
      `js/app.js`) במקום `title` שנשבר עם גרשיים ולא עבד באמינות במגע. מתגי חומרא
      (9 מחלוקות נדירות) קופלו כברירת מחדל ב-`<details>`, ונפתחים אוטומטית רק אם
      מתג שונה מברירת המחדל שלו (לא סתם "דלוק"). `npm test` עבר במלואו, ואומת ידנית.
      ראו `docs/AGENT_LOG.md` ו-`docs/DECISIONS.md`.
- [x] **סבב עיצוב ממשק — סדר, קיבוץ ותצוגה (2026-09-18):** הוסתרו כלי ניווט הלוח/דשבורדים
      בלשוניות שאינן "לוח שנה" (`calendar-view-active` על ה-`body`); כרטיסי הדשבורד קובצו
      חזותית (`.dashboard-stack`); מסך ההגדרות פוצל ל"הלכה וחישוב" מול "חשבון, פרטיות
      וגיבוי" ברשת דו-טורית, ו"מצב חיים" עטוף כ-`<details open>`; מודאל פעולות היום פוצל
      לפעולות שכיחות גלויות + שלוש קבוצות מתקפלות; מקרא הסימונים בלוח קוצר (הופנה למדריך
      ההלכתי); סדר הלשוניות אוחד ("מדריך הלכתי" לפני "אודות"); כפתורים קיבלו מחלקות צבע
      סמנטיות (`.btn-yellow`/`.btn-orange`). `npm test` עבר במלואו (19/19), ואומת ידנית
      בדפדפן. ראו `docs/AGENT_LOG.md` ו-`docs/DECISIONS.md`.
- [x] מנוע חישוב: עונה בינונית ל' (`עו"ב`) ול"א (`עו"ל`), הפלגה (`עו"ה`), יום החודש (`יו"ח`), אור זרוע (`עוא"ז`)
- [x] מחלוקת יום החודש בראייה ב-ל' כשהחודש הבא חסר — סימון שני תאריכים (`יו"ח*`) והודעה "יש לשאול רב"
- [x] הפסק טהרה, שבעה נקיים וטבילה (כולל ביטול הפסק שנקטע)
- [x] תיקון ניווט חודשים על פני ראש השנה (`shiftHebrewMonth`) ואימות תאריך קיים (`buildExactDay`)
- [x] גיבוי אוטומטי לגוגל שיטס + שחזור (OAuth)
- [x] **הכנה להפצה נקייה:** פרטי ה-OAuth (`clientId` + `clientSecret`) מוטבעים ב-`main.js`, ומסך הזנת הסוד הוסר מהממשק יחד עם ערוץ `oauth-save-credentials` — המשתמש רק לוחץ "חבר חשבון גוגל"
- [x] מערך בדיקות: `engine.test.js`, `googleBackup.test.js`, `integrity.test.js`
- [x] **תווית ההפלגה** — מוצג המניין ההלכתי (`abs[last] - abs[prev] + 1`), התאריך לחישוב לא השתנה (`[ד"ט | עמ' 4]`)
- [x] **אימות מפת הפרקים של הספר (2026-09-17):** הוערת שהקובץ מתחיל ב**פרק כ"ב** —
      ובצדק. חולצו **הכותרות הרצות** שאבדו בכלי המקורי, וזוהתה **מפת הפרקים המלאה:**
      כ"ב (עמ' 1–23) · כ"ג (24–63) · כ"ד (64–73) · כ"ה (74–77) · כ"ו (78–91) ·
      כ"ז (92–101) · כ"ח (102–107) · כ"ט (108–140) · ל' (141–157) · ל"א (158–181) ·
      ל"ב (182–189).      **הקובץ מכיל את כל ספר דיני הוסתות — לא חסר ממנו מאומה.**
      ראו `docs/SPEC_DINIM_VESATOT.md` §0.3.
- [x] **וסת השבוע (2026-09-17):** `js/chazaka.js` + הקרנה ב-`וק"ש` + עקירה + חזרה מן הסילוק.
      הזיהוי דורש זהות יום-בשבוע ועונה **בהפרש שבוע בדיוק** — ראו `DECISIONS.md`. `tests/vesetWeek.test.js`.
- [x] **משיכת הראייה (2026-09-17):** `js/reiyaDuration.js` — החששות נכתבים גם בעונות הסמוכות
      שהדימום נמשך בהן, ומניין הימים מן הראייה שבה החל הדימום. `tests/reiyaDuration.test.js`.
- [x] **יו"ח בחודש חסר — הדעה השלישית (2026-09-17):** שלושה תאריכים מסומנים וממוינים, בקוד `יו"ח*`.
- [x] **סוג הכדורים בחשש יום ההפסקה (2026-09-17):** הדין נאמר על "רוב הכדורים", ולמעטם
      "כדורי אורגסט, שתלוי בסדר שנוטלים אותם" `[שט כ"ז | עמ' 40]` — נוסף קלט הסוג,
      ואין מחשבים חשש להפסקה מכדורי אורגסט. `js/lifeState.js` · `js/pillPause.js` · `js/ui.js`.
- [x] **בועות מידע — כיסוי מלא (2026-09-17):** בועה לכל עמודה בטבלת הריכוז ולכל הערך בה,
      ומקרא סימונים מלא ללוח. (בסבב "סימוני יום" נוספו שלושה נושאים ובועותיהם —
      "סימוני היום", "מאכל חריף", "פטורי אור זרוע"; סה"כ 38 נושאים ו-48 בועות.)
- [x] **"צירוף למפרע" — קביעת וסת ההפלגה על פני הפלגה קצרה (2026-09-17):**
      `detectChiburLemafrea` (`js/chazaka.js`) + מתג `chiburLemafrea` (`js/stringencies.js`),
      הצגת התבנית בפאנל עם לשון המקור, נושא הסבר חדש (`chibur_lemafrea`),
      ו-`tests/chiburLemafrea.test.js`. ראו `docs/DECISIONS.md`.
- [x] **מסמך שאלות לרב (2026-09-17):** `docs/RABBI_QUESTIONS.md` — כל הפערים הפתוחים
      והכרעות ברירת המחדל, בשאלות מנוסחות עם מראי מקום.
- [x] **סימוני יום וחמשת הדינים התלויים בהם (2026-09-17):** `js/dayMarks.js` — **פטורי
      עונת אור זרוע** (ליל טבילה · חופה · יוצא לדרך), **כתם** (אינו קובע ואינו עוקר;
      מחלוקת העקירה במתג `stainUproots`), **פחד פתאום וחרדה** (מתג `frightBedika`),
      **מאכל חריף בלא ראייה** (A6), ו**מתג מניין ההפלגה** (`haflagahFromEnd`). הסימונים
      נשמרים על כל סוג רשומה, מוצגים בלוח ובפאנל עם מראה מקום, ונשמרים בגיבוי ובשחזור
      (עמודה בשני הטאבים). ראו `docs/DECISIONS.md`.
- [x] **כלי אימות מראי מקום (2026-09-17):** `tools/verify_citations.mjs` (`npm run verify:sources`)
      — גוזר מן התמלול את טבלת העמודים של כל שיעור ובודק כל ציטוט `[שט N | עמ' P]` בקוד
      ובתיעוד. תוצאתו הראשונה — 136 ציטוטים שמספר השיעור שבהם אינו מתיישב עם הטבלה.
      **הורחב (2026-09-17):** תומך גם בציטוטים שבתוך מחרוזות JS ובציטוטי כמה עמודים
      (880 ציטוטים במקום 600), מדפיס את מפת השיעורים, מציע תיקון — ויודע לכתוב אותו
      (`--dry-run` · `--fix` · `--strict`). ההכרעה והתיקון — ראו "תפעול והפצה" להלן.

---

## ממתין

- [ ] **ניסוי חי של החיבור לגוגל** (2026-09-17): האפליקציה מותקנת, בנויה ומופעלת (חלון אחד).
      הסוד התקני (`GOCSPX-`) הועבר ל-userData, וההיקף צומצם ל-`drive.file` בלבד. נדרש אימות:
      התחברות → יצירת גיליון "לוח טהרת המשפחה - גיבוי" → גיבוי ראשון → שחזור לאחר ניתוק.
- [x] אימות שהלקוח בפרויקט גוגל הוא מסוג **Desktop app** — אושר מתוך מסך הקונסולה (`Client ID for Desktop`, `installed` בקובץ ההורדה).
- [x] אימות נוכחות **Client secret** תקני — נמצא ונכתב ל-`userData/google-oauth-client.json` (`GOCSPX-`, אורך 35).
- [x] **צומצם ההיקף ל-`drive.file` בלבד** — להסרת מסך האזהרה "Google hasn't verified this app". **הסר גם את `.../auth/spreadsheets` ממסך ה-Scopes בפרויקט גוגל**, אחרת האזהרה תמשיך להופיע.
- [ ] **בדיקת התקנה נקייה (הפצה):** להתקין את הבנייה החדשה על פרופיל משתמש ריק (`%APPDATA%\purification-board` ריק או משתמש אחר) ולוודא שהחיבור לגוגל עובד בלי להזין סוד — ואז לבנות מחדש את המתקין (`npm run dist`).
- [ ] **לתקן את הסוד בקונסולה:** בקונסולה מוצג סוד שמסתיים ב-`kblR` (לא ניתן לצפייה), ואילו הסוד שנמצא בהורדות מסתיים ב-`XQ3v` — ייתכן שנוצרו שני סודות. אם ההתחברות תיכשל ב-`invalid_client` — ליצור סוד חדש דרך **Add secret** ולשמור אותו מיד.
- [ ] במצב Testing של מסך ההסכמה — הוספת חשבון המשתמש ל-**Test users** וזכרון שגוגל מנתקת את הטוקן כל 7 ימים.
- [x] **הסרת `.../auth/spreadsheets` ממסך Data Access בקונסולה** — למשתמש היו הוראות מפורטות; האזהרה לא תיעלם כל עוד ההיקף מוצהר שם.
- [x] **אבחון "הסיסמה שגויה"** (2026-09-17): הפין השמור מתפענח ל־**976061**. נשלל חשש התקנה כפולה — `%LOCALAPPDATA%\Programs\purification-board` ריקה (0 קבצים).
- [x] **תוקן שלב 6 ב-`GOOGLE_OAUTH_SETUP.md`:** ההוראה למחוק את תיקיית ה-data הוחלפה באזהרה
      מפורשת שאין למחוק אותה (היא מכילה את כל נתוני המשתמש), ובהפניה לניתוק החשבון בלבד.
- [ ] ירושה טכנית: אין כלי מובנה למשתמש לשחזר/לאפס קוד גישה — הפין נשמר ב-localStorage מוצפן, ורק מעקף ידני מאפשר איפוס.

---

## פערים הלכתיים שזוהו (מפת דרכים)

הפערים להלן נגזרו ממסמך הסיכום ההלכתי שנמסר על ידי המשתמש, ומולם נבדק בפועל מה
המנוע מחשב היום. כל סעיף מסומן לפי מורכבות המימוש.

### א. וסת קבוע, וביטול שאר החששות — פער מרכזי

**מצב היום:** המנוע מחשב את שלושת החששות (חודש, הפלגה, עונה בינונית) עבור **כל**
ראייה, ללא זיהוי קביעות וללא צמצום. כלומר המערכת תמיד מחמירה ואינה מזהה וסת קבוע.

**חסר:**
- זיהוי קביעות וסת החודש: 3 ראיות רצופות באותו תאריך עברי ובאותה עונה.
- זיהוי קביעות וסת ההפלגה: 4 ראיות רצופות שיצרו 3 הפלגות שוות.
- זיהוי קביעות וסת הגוף: 3 ראיות רצופות עם אותו סימן גופני (דורש קלט חדש).
- תנאי "ממעיין סתום": הראיות חייבות להיות נפרדות ולא דימום רציף (דורש קלט חדש).
- כאשר יש וסת קבוע — לחדול מחישוב שאר החששות, ולהציג רק את זמן הוסת הקבוע.

**הערה:** הוספת חומרא אינה איסור, אך היא אינה מדויקת הלכתית ומקשה על המשתמשת.

### ב. עקירת וסת

- וסת שאינו קבוע שעבר זמנו ולא ראתה — נעקר מיד. המנוע כיום ממשיך להציגו.
- וסת קבוע נעקר רק לאחר 3 זמנים רצופים בלא ראייה, ובצירוף חובת בדיקה.
- החזרת וסת קבוע לאחר עקירה — מחלוקת הפוסקים (פעם אחת או 3 פעמים).

### ג. נשים מסולקות דמים

**מומש (2026-09-17)** — `js/lifeState.js` + "מצב חיים" בהגדרות. ראו `DECISIONS.md`.

- מעוברת: 3 חודשי הריון מלאים (90 יום) — מסולקת דמים; בשלושת החודשים הראשונים עדיין חוששת. ✅
- מניקה: 24 חודשים מהלידה (בזמננו — אם הפסיקה להניק וחזרה לראות, חוששת כרגיל). ✅
- זקנה: עברו עליה 3 עונות בלא ראייה. ✅
- קטנה: עד שתראה שלש פעמים (`[שט ל' | עמ' 73]`). ✅
- כדורים: **ההצעה שתוארה כאן (“בזמן נטילה סדירה — מסולקת דמים; בהפסקה — חישוב מחדש”)
  לא אומתה במקור, ולכן לא מומשה.** מה שכן אומת: הכדורים גורמים לכך שהוסת הקודם
  **אינו נעקר** מחמת הפלגה ארוכה, "כיון שניכר בבירור שהפלגתה הארוכה מחמת הכדורים"
  `[ד"ט | עמ' 8]` — וזה מה שמומש (עיכוב עקירה).

### ג2. נשים מסולקות דמים — מה שהוצג במפורש כדחוי

- [x] **חזרה מן הסילוק — הושלם ומיושם (2026-09-17):** `js/silekReturn.js`. שני הכללים
      שיושמו: (א) “היה לה וסת קבוע קודם נטילת הכדורים — חוזרת לוסתה הקבוע הקודם, אבל לא
      לוסת שאינו קבוע”, ואף אם הכדורים קבעו וסת אחר — “אחר שהפסיקה חוזרת לוסתה הראשון”
      `[שט כ"ז | עמ' 42]`; (ב) בחלוקת הוסתות ביציאה מן ההריון ומן ההנקה — וסת הימים חוזרת
      **מיד**, “אבל אם היה וסתה וסת ההפלגה אי אפשר לחוש עד שתחזור לראות” `[שט כ"ט | עמ' 71]`.
      החזרה מעוגנת מתום הסילוק (ולא מן הראייה שמכוחה נקבעה הוסת), וראיות שבתוך ימי הסילוק
      אינן קובעות וסת. **עוד פתוח בהמשך הסעיף:**
- [x] **חשש יום ההפסקה בכדורים** `[שט כ"ז | עמ' 40–41]` (A5), ופטור מעונת אור זרוע בוסת
      מכדורים `[שט כ"ז | עמ' 40]` (A7) — **מומש (2026-09-17):** `js/pillPause.js` + הזרקה
      ללוח ב-`js/calculations.js` (סימון `פה"כ`) + פאנל במצב חיים. מה שמומש: (א) יום ראשון
      מותר, וחשש בימים ב'–ה' שאחרי ההפסקה; (ב) היום שקבעה לעצמה (וסת שאינו קבוע, ובג'
      פעמים וסת "קבועה" שאינה גמורה); (ג) החששות אינם יוצרים דרישת בדיקה ואינם מבטלים
      חששות אחרים; (ד) אין אור זרוע לחששות של יום ההפסקה ולראייה שסומנה כמחמת כדורים.
- [x] **הבחנה בין סוגי הכדורים בחשש ההפסקה (A5ב)** — **הושלם ומיושם (2026-09-17):** אומת
      בתמלול הספר: "כן הוא **ברוב הכדורים**"... "**מלבד כדורי אורגסט, שתלוי בסדר שנוטלים
      אותם**" `[שט כ"ז | עמ' 40]`. נוסף סוג `orgast` + `pauseRuleOfPillType()`
      (`js/lifeState.js`); הפסקה מסוג זה **אינה מחשבת חלון ואינה קובעת יום** — מוצגת
      בפאנל ובהגדרות עם לשון הדין ומראה המקום, בגדר בירור עם רב (`js/pillPause.js`,
      `js/ui.js`). עוד מוצג בתנאי היסוד של הדין (`[שט כ"ז | עמ' 39]`) ובקביעות המצטרפת
      מכמה סוגי כדורים (`[שט כ"ז | עמ' 41]`). ראו `docs/DECISIONS.md`.

### ד. ספק עונה

אישה שראתה בסמוך לנץ או לשקיעה ומסופקת באיזו עונה.

**תוקן (2026-09-17) לפי המקור:** אינה חוששת לשתיהן אלא **לעונה המאוחרת בלבד**
(אם הספק בין לילה ליום — היום אסור): "אינה אסורה **אלא ביום** כיון דוסתות דרבנן
וספק דרבנן לקולא" `[שט כ"ו | עמ' 27–28]`, וכן ב"דעת טהרה" (§9.1).
השיטה המחמירה (לחוש לשתי העונות) — **מתג חומרא**, לא ברירת מחדל.

מימוש: סימון "ספק עונה" על הראייה → הפרישה מסומנת בעונה המאוחרת, עם הסבר בממשק.
מימוש מלא של עונה אוטומטית ידרוש זמני הנץ/שקיעה לפי מיקום — תלוי החלטת המשתמש.

### ה. וסתות נוספים (סעיפים 3 במסמך)

- ~~וסת השבוע~~ — **הושלם (2026-09-17)**; ===וסת הסירוג=== — **מומש (2026-09-17):**
  `js/vesetDilug.js` מזהה תבנית "דילוג חלילה" (מחזור ב'–ג' ימים), והמנוע **מציג** אותה
  לבירור עם רב; חשש מחמתה — רק במתג החומרא `dilug`. ראו `DECISIONS.md`.
- ~~וסת הדילוג: דילוג החודש (הזזה קבועה של היום בכל חודש)... **טרם מומש.**~~
  **מומש (2026-09-17):** אותו מודול — מחזור מלא החוזר פעמיים שלמות, בחודשים עוקבים ובאותה
  עונה, והקרנה ב-`וק"ד`. **דילוג ההפלגה (הפרשים גדלים/קטנים בקבוע) — טרם מומש**, ומטעמו
  של הספר (וסתות שאינן שכיחות) הוא מן הפריטים שלא ימומשו בלא הכרעת רב.
- ~~וסת הגוף: סימן גופני קבוע~~ — **הושלם (2026-09-17):** `js/vesetGuf.js`.
- ~~ימי המבוכה / קבוצת ימים: טווח ימים במקום תאריך — מחלוקת הפוסקים באילו ימים לחוש.~~
  **מומש בצמצום (2026-09-17):** `findAlternatingDays` ב-`js/chazaka.js` מזהה את הצורה
  המפורשת שבמקור (שני ימים שהפרשם שני ימים, ג' ראיות בכל אחד באותה עונה, והיום המפסיק
  לא נראה), וממנה נקבע וסת לימים המתחלפים — סימון `וק"מ`. תבנית של "טווח ימים" כללי
  **לא** זוהתה, מחמת אזהרת הספר מפני וסתות שאינן שכיחות.
- **"יום י"א / י"ב / י"ג בוסתות מיוחדים"** — הפריט רשום כאן בלא פירוט, **לא אותר
  ככלל מפורש בגוף הספר, ולא מומש**. ראו `docs/SPEC_DINIM_VESATOT.md` §10.1(8).

> **מצב הפערים בסעיף זה (2026-09-17):** כל הפריטים שמעל מומשו, **למעט** סעיפים (1)–(2)
> לעיל (מאכל חריף בפעם אחת, וקטנה/זקנה). **הרשימה המאומתת המלאה של מה שלא מומש** —
> `docs/SPEC_DINIM_VESATOT.md` §10.1.

### ו. פערים שהתגלו בעיון בספר המקור (חדש — 2026-09-17)

אלה לא היו ידועים לפני פענוח "שיעורי טהרה":

- **עקירת וסת ההפלגה:** וסת הפלגה אינו נעקר בדרך הרגילה (הגעת זמן וסת בלא ראייה),
  אלא ב**שינוי מניין ההפלגות**, או במעבר "שלש עונות" בלא ראייה — כש"יום הראייה
  עולה לכאן ולכאן". לחישוב: `periodsToClear(span) = span * 3 - 2`.
  (דוגמה מהספר: וסת של 20 יום → 58 יום.) `[שט מ"א | עמ' 183]`
- **וסת הסירוג:** הספר מציג מחלוקת אם "דילוג חלילה" הוא וסת **אחד** (נעקר
  בפעם אחת) או **ג' וסתות סירוג** (נדרש לעקור כל אחד מהם בנפרד). נפק"מ מעשית
  ישירה באלגוריתם העקירה. `[שט ל"ז | עמ' 149]`
- **וגם:** דין "עבר הוסת המיועד בוסת הדילוג" — בוסת החודש חוששת לחודש הבא כאילו
  ראתה, ובוסת ההפלגה חוששת לשני המניינים. `[שט ל"ז | עמ' 149]`
- **שדה קלט חסר — "דימום רציף / ראיות נפרדות":** הספר מייחס חשיבות לשאלה אם
  הראייה היתה דימום רציף או ראיות נפרדות ("ממעיין סתום"), וזו **אבן יסוד
  לקביעות ולעקירה**. בלעדיה לא ניתן לממש קביעות נאמנה.
- ~~**פטורים מעונת אור זרוע:** הספר מונה ששה מקרים שבהם חומרת האור זרוע אינה
  נוהגת (ליל טבילה, ליל החופה, יוצא לדרך — ובפרט ביום הל"א)~~ — **מומש (2026-09-17):**
  `js/dayMarks.js` (`orZaruaExemptionFor`); העונה שנפטרה נאספת ומוצגת. ראו
  `docs/SPEC_DINIM_VESATOT.md` §2.5 ו-`docs/DECISIONS.md`.

### ז. דיוקים ובדיקה חוזרת

- ~~**תצוגת ההפלגה:** המנוע מציג את **הפרש הימים** (27) במקום המניין ההלכתי (28).~~
  **נסגר (2026-09-17).** המנוע מציג כעת את המניין ההלכתי (`diff + 1`); התאריך
  לחישוב החשש לא השתנה. `[ד"ט | עמ' 4]`
- ~~**מחלוקת יום החודש:**~~ **נסגר (2026-09-17):** שלוש הדעות מסומנות (כ"ט בחודש החסר · ל'
  בחודש מאוחר · **א' בחודש הבא** בתורת ראש חודש), ואין מכריעים. ראו `DECISIONS.md`.
- **וסת החודש בחודש חסר:** המסמך מציין דעה שאינה חוששת כלל באותו חודש קצר — יש לשקול
  הצגת שלוש החלופות במפורש.

---

## בביצוע

**אושר למימוש על-ידי המשתמש ב-2026-09-17.** סדר העבודה נגזר מהתלויות (קלט לפני מנוע):

- [x] **א. מצב חיים (B1 / פער 4)** — **הושלם ומיושם (2026-09-17):** `js/lifeState.js` — הריון מתאריך,
      לידה והנקה, גיל, וסוג כדורים עם תקופות נטילה; פאנל `#life-container` והגדרה מלאה.
      השלכות: (א) חששות שהיו לה קודם הסילוק מבוטלים ומדווחים; (ב) מסולקת דמים פטורה מבדיקה;
      (ג) ימי הכדורים אינם נמנים לעקירה. ראו `DECISIONS.md`.
      **עוד פתוח בהקשר זה:** (1) ~~**חזרה מן הסילוק**~~ — **הושלם ומיושם (2026-09-17):**
      `js/silekReturn.js` — ראו סעיף ג2 לעיל; (2) **חשש יום ההפסקה** בכדורים
      `[שט כ"ז | עמ' 40–41]`; (3) **פטור מעונת אור זרוע בוסת מכדורים** `[שט כ"ז | עמ' 42]`;
      (4) קטנה וזקנה “אין חוששת לוסת שאינו קבוע” (שו"ע קפ"ט סכ"ח) —
      חומרה שלא מומשה (המנוע מציג חששות לקטנה שראתה ג' פעמים).
- [x] **ב. לוחית בדיקות (B4 / פער 3)** — **הושלם ומיושם (2026-09-17):** סוג אירוע `check` עם העונה
      והעומק (בדיקה כדין / קינוח בלבד); נשמר בגיבוי ובשחזור (טבלה ראשית + היסטוריה). "בדקה כדין"
      מאפשר עקירת וסת קבוע, ו"לא בדקה" מוצג כ**ממתין לבדיקה** — עם הדין: אסורה עד שתבדוק.
      **עוד פתוח:** שתי בדיקות ביום (לפני שקיעה ואחריה) — כרגע רשומה אחת לעונה.
- [x] **ג. מנוע מבוסס-חזקה (פער 1)** — **הושלם ומיושם (2026-09-17):** `js/chazaka.js` — חזקת ג' ראיות
      באותו יום ובאותה עונה (חודש) וד' ראיות בג' הפלגות שוות (הפלגה); ראייה מחמת אונס אינה נספרת,
      ומשך הדימום מכריע אם מדובר בראייה חדשה; וסת קבוע מחליף את שאר החששות. ראו `DECISIONS.md`.
- [x] **ד. עקירת וסתות (פער 2)** — **הושלם ומיושם (2026-09-17):** `js/akira.js` — וסת שאינו קבוע
      נעקר מיד; וסת קבוע בג' זמנים רצופים **ובצירוף בדיקה כדין**; וסת הפלגה גם במניין ימים
      (`span*3-2`). נוסף גם **מצב הבטיחות** ("אסורה עד שתבדוק") ו**מחלוקת חזרת הוסת** (מוצגת,
      לא מוכרעת). ראו `DECISIONS.md`.
- [x] **תלות מנוע העקירה — מצב חיים (B1 / פער 4)** — **נסגרה (2026-09-17):** פטור מבדיקה
      למעוברת/מניקה/זקנה/קטנה, ועיכוב עקירה בכדורים (ימי נטילה + ראייה מחמת כדורים)
      `[ד"ט | עמ' 8]`. ראו ההחלטה על מצב חיים ב-`docs/DECISIONS.md`.
- [x] **כללי העונות המעורבות שבסעיף 3.1** — **מומש (2026-09-17):** "ג' ביום והרביעית
      בלילה → חוששת ליום וללילה" `[שט ל"ג | עמ' 112–113]`: הזיהוי ב-`mixedOnaChange()`
      שבג'`js/chazaka.js`, והחשש שבעונה שכנגד מסומן בלוח בסימון `עו"מ` (`js/calculations.js`)
      עם הצגה בפנל ובכרטיס ההסבר. **ההכרעה שהתקבלה:** דין הצמצום "עונות מעורבות שלא
      על הסדר → חוששת לאחרונה בלבד" **אינו** ממומש — המערכת אינה מסירה על דעתה חששות
      של ראיות שתועדו; ראו `docs/DECISIONS.md`.
- [x] **ה. בועות מידע עם מקורות (בקשת המשתמש)** — **הושלם (2026-09-17):** 35 נושאי הסבר
      עם מקורותיהם (`js/halachaHelp.js`), המרנדרים גם כבועות מידע (`data-help`) — 43 בועות
      בממשק; כל ראייה וכל זמן וסת נושאים הסבר ומראה מקום בטקסט של היום.
      **הושלם בסבב זה:** בועה בכל אחת מחמש עמודות טבלת הסיכום ובהערות שבערכים (המחלוקת
      ב-`יו"ח*`, "ממנה נקבעת הוסת", "לא נספרת לחזקה", מיחוש) — `renderSummaryTable`;
      ו**מקרא סימונים מלא ללוח** (`<details class="board-legend">` ב-index.html) — כל סימון
      וכל קוד פרישה עם בועה משלו; ונוספו הנושאים החסרים: וסת השבוע, משיכת הראייה, פרישה,
      סימון הראייה, הפסק טהרה, נקיים, טבילה, הערה אישית וחשש שבוטל (`וק"ב`).
      *(מספרי הכיסוי עודכנו מאז ל-38 נושאים ו-48 בועות — ראו "סימוני יום" לעיל.)*

**עקרון מנחה שאושר:** הצגת **הדין המדויק בלבד** — לא להחמיר ולא להקל; מחלוקת פוסקים
מוצגת כמחלוקת מפורשת ולא מוכרעת בשקט. ראו `docs/DECISIONS.md`.

### קלט חדש שאושר (B1–B6, סבב 3)

- [x] **B3 סוג הראייה** — **הושלם ומיושם (2026-09-17):** ארבע אפשרויות (רגילה / אונס-קפיצה / מאכל חריף / כדורים) + בועת הסבר; נשמר בגיבוי ובשחזור.
- [x] **B5 משך הראייה (ממעיין סתום)** — **הושלם ומיושם (2026-09-17):** שאלת משך הדימום עם מספר ימים + בועת הסבר; נשמר בגיבוי ובשחזור.
- [x] **B1 מצב חיים** — **הושלם ומיושם (2026-09-17):** הריון / לידה והנקה / גיל / כדורים, על השלכותיהם.
- [x] **A5 + A7 יום ההפסקה בכדורים ופטור מאור זרוע** — **הושלם ומיושם (2026-09-17):**
      `js/pillPause.js` — יום ראשון מותר, ימים ב'–ה' בחשש, היום שקבעה לעצמה, ואין אור זרוע
      לחששות מכדורים. ראו `docs/DECISIONS.md` (2026-09-17, יום ההפסקה בכדורים).
- [x] **B2 מיחוש גופני** — **הושלם ומיושם (2026-09-17):** `js/vesetGuf.js` — רשימת המיחושים (סימני המשנה והסימנים שהוסיפו הראשונים), סימון על הראייה במודאל (מותנה באישור שהמיחוש משונה וקשור), שמירה ב-db ובגיבוי (עמודה בטאב הראשי ובטאב ההיסטוריה), וסימון `מיחוש` ביום הראייה בלוח. ראו `docs/DECISIONS.md`.
- [x] **תזכורת יומית לוסת הגוף (המיחוש הממתין והבדיקה הנדרשת)** — **הושלם ומיושם (2026-09-17):**
      `bodyReminder()` ב-`js/vesetGuf.js`; זמן הווסת המורכבת שעבר בלא בדיקה נכנס ל-`pendingChecks`
      עם `kind: 'body'` ותובע בדיקה כדין — **גם כשוסת קבועה של ימים עומדת**; הכרטיס שבראש המסך
      מציג pending/today/armed, התראת toast פעם ביום (`taharahBodyReminderSeen`), ומודאל היום
      מציג את דין המיחוש. ראו `docs/SPEC_DINIM_VESATOT.md` §3.6.2 ו-`docs/DECISIONS.md`.
- [x] **רישום מיחוש בלא ראייה** (פער קלט מוצהר) — **מומש (2026-09-17):** `js/vesetGuf.js`
      (`signRecords`) + `js/calculations.js` + מודאל נפרד בממשק; המיחוש נרשם היום רק עם ראייה, ולכן מיחוש
      שיבוא לבדו אינו ידוע למנוע ואין לו תזכורת מתוארכת — ראו מגבלה מוצהרת ב-`docs/DECISIONS.md`.
- [x] B4 לוחית בדיקות · B6 ספק עונה + הצעת זמנים (מהספרייה הקיימת) — **מומשו (2026-09-17):**
      לוחית הבדיקות (סוג, עונה, עומק, ו"פעמיים בעונה"), סימון ספק עונה על הראייה, וזמני נץ
      ושקיעה לפי מיקום (`js/zmanim.js`).
- [x] **הערה על B3/B5 — נסגרה (2026-09-17):** המנוע צורך את שני הקלטים. `kind=ones` אינו נספר;
      `sharp`/`pills` נספרים (כוסת הגוף); וראייה שסומנה כ"המשך" נמנית עם הראייה שקדמה לה ("ממעיין
      סתום"). סימון "המשך" שסותר את מספר הימים שצוין אינו ממוזג — הראייה נספרת כראייה נפרדת
      ומוצגת אזהרה. ראו `docs/DECISIONS.md` (2026-09-17, מנוע החזקה).

### כללים מאומתים (A1–A11) ומה שביניהם

- [x] **A1–A11** — 11 כללים מאומתים במקור (`SPEC_DINIM_VESATOT.md` §11.4): וסת הגוף, וסת מורכב, שתי בדיקות בעונת הוסת, פטור בדיקה למסולקת, כדורים ויום ההפסקה, מאכלים חריפים,
      יציאה מסילוק, ימי המבוכה, תנאי העונה בג' ראיות. **A1 ו-A2 הושלמו (2026-09-17)** — `js/vesetGuf.js`; **A3 נסגר (2026-09-17)** — "בדקתי פעמיים בעונה זו" בלוחית הבדיקה (`tests/checkTwice.test.js`); **A9 נסגר (2026-09-17)** — `וק"מ`; **A6 נסגר (2026-09-17)** — `sharpFood` כרשומה העומדת לעצמה.
- [ ] **C1–C3** — שלוש הצעות שהומלץ **לא** לממש אוטומטית (`§11.6`), מסיכון הלכתי.

---

## ממתין

### פערים הלכתיים פתוחים — הרשימה המאומתת

**הרשימה המלאה, שנבדקה מול הקוד ולא מול התיעוד, נמצאת ב-`docs/SPEC_DINIM_VESATOT.md`
§10.1.** ראשי הפרקים:

- [x] ~~**ששת פטורי עונת אור זרוע**~~ (ליל טבילה · ליל החופה / בעילת מצוה · יוצא לדרך · יום
      הל"א) `[שט כ"ז | עמ' 49]`, `[שט כ"ח | עמ' 57]` — **הושלם (2026-09-17):**
      `js/dayMarks.js` — ליל טבילה נגזר מן הטבילה שתועדה, וחופה/דרך מן הסימון על היום.
- [ ] **קטנה וזקנה — "אין חוששת לוסת שאינו קבוע"** (שו"ע קפ"ט סכ"ח) — המנוע חושש להן כרגיל.
- [x] ~~**חשש "בפעם אחת" מחמת המאכל החריף עצמו (A6)**~~ — **הושלם (2026-09-17):**
      "אכלתי מאכל חריף" נפתח כמועד העומד לעצמו ונשמר כרשומת מיחוש בלא ראייה
      (`standaloneSign`), ודינו כוסת הגוף — אף בפעם אחת.
- [x] ~~**קלט "הפסיקה מחמת רופא / מדעתה"** (§5.1)~~ — **קיים (אומת בקוד 2026-09-17):**
      `PILL_PAUSE_REASONS` + `PILL_PAUSE_REASON_RULE` (`js/lifeState.js`), נשמר עם תקופת
      הכדורים ומוצג עם לשון הדין. **ואינו משנה את החישוב — זו הכרעת הספר עצמו** (עמ' 41).
- [x] ~~**"קודם שקבעה — ראיית הכתם כעקירה"** (§4.3)~~ — **הושלם (2026-09-17):** סימון
      **כתם** (אינו נספר לקיבוע), ומחלוקת העקירה במתג `stainUproots` — ברירת המחדל כהפרישה.
- [x] ~~**חרדה וביעתותא** (§5.9)~~ — **הושלם (2026-09-17):** סימוני `fright` / `anxiety`
      עם לשון הדין, ומחלוקת הבדיקה במתג `frightBedika` (ברירת המחדל כחת"ס).
- [ ] **דילוג ההפלגה** — במכוון לא מומש (אזהרת הספר מפני וסתות שאינן שכיחות).
- [ ] **הדעה הרביעית ביו"ח בחודש חסר** ("אינה חוששת כלל באותו חודש קצר") — מוצגות שלוש דעות.
- [x] ~~**בדיקה באיחור — מחלוקת הב"י והחוות דעת** — מיושם כהב"י, ואינו מוצע כמתג חומרא~~ —
      **מוצע כמתג (אומת בקוד 2026-09-17):** `lateBedika` (דלוק = כהב"י; כיבויו מחייב בדיקה
      בזמן הוסת עצמו, `requireSameDay` ב-`js/akira.js`).
- [x] ~~**מחלוקת "מתחילת ראייה מול סוף ראייה"** במניין ההפלגה — אינה מוצגת למשתמשת~~ —
      **הושלם (2026-09-17):** מוצגת כמתג חומרא `haflagahFromEnd` (ברירת המחדל מתחילת הראייה).

**נוספו בביקורת החוזרת (2026-09-17) — פערים שלא היו ברשימה:**

- [x] ~~**"אין וסת קצר עוקר וסת ארוך" (צירוף למפרע)** (§3.1, `[שט ל"ג | עמ' 112]`)~~ —
      **הושלם (2026-09-17):** `detectChiburLemafrea` (`js/chazaka.js`) מזהה את התבנית;
      היא **מוצגת תמיד** בפאנל עם לשון המקור, והחישוב במתג `chiburLemafrea` בלבד (כבוי),
      מחמת שהספר מגדיר את הכלל "רק לחומרא בעלמא ולא מעיקרא דדינא".
      `tests/chiburLemafrea.test.js`. ראו `docs/DECISIONS.md`.
- [ ] **"כתמים כסימן" — וסת הגוף של כתמים** (§3.6, `[שט ל"ט | עמ' 159]`): דינה של אשה
      ש**דרכה** לראות כתמים — "אסורה בביאת הכתמים כדין וסת הגוף... ויש להקל משאר וסת
      הגוף" — לא מומש. יש להבדילו מן הכתם שאינו דבר (§4.3).
- [ ] **"עונה בינונית למעוברת שראתה — נחלקו בזה האחרונים"** (§5.2, `[שט כ"ט | עמ' 69]` ⚠️) —
      אינה מוצגת בשום מקום בממשק.
- [ ] **"דעה מקילה בדיעבד" בתנאי העונה** (§2.2, `[שט ל"ו | עמ' 137]`) — המנוע נוקט את
      השיטה המחמירה, וזה דעת הרוב, אך המחלוקת אינה מוצגת.
- [ ] **עקירת ההפלגה הארוכה מחמת קביעות קצרה** (§3.1, `[שט ל"ג | עמ' 112]`): הספר אומר
      "ודאי דעקרה החשש להפלגה הארוכה", ואף דין זה "מעיקר הדין"; והמערכת מחמירה ומשאירה
      את הארוכה עומדת. **הקלה שלא מומשה** — ראו `docs/RABBI_QUESTIONS.md` שאלה 7.

> **כל הפריטים הפתוחים ⁃ ריכוז לדיון עם רב:** `docs/RABBI_QUESTIONS.md` — שאלה מנוסחת
> לכל אחד מהם, עם הדין, מראה המקום, ומה המערכת עושה היום. **הכרעות ברירת המחדל שבמתגי
> החומרא מרוכזות בנספח ב' שם.**

### תפעול והפצה

- [x] **אימות מספרי השיעורים בציטוטים** — **הוכרע ותוקן (2026-09-17).** `npm run verify:sources`
      גוזר מן התמלול את גבולות השיעורים, ובדיקה חוזרת מול קובץ ה-PDF (בקואורדינטות)
      אישרה ששורות `* שיעור ...` הן **כותרות השיעור המודפסות בגוף הספר** — אחת לכל שיעור,
      בעמוד שבו השיעור מתחיל (מ"כ"ד בעמ' 1 עד מ"א בעמ' 182). מכאן: **העמוד הוא הוודאי
      ומספר השיעור הוא שנתעה.** תוקנו **223 ציטוטים** בשש קבוצות של הסטת שיעור אחד.
      מפת השיעורים המלאה — `docs/SPEC_DINIM_VESATOT.md` §0.3.1; ההכרעה — `docs/DECISIONS.md`.
- [x] **19 הציטוטים שנותרו לעיון — נסגרו (2026-09-17, סבב שני).** כל אחד מהם הוכרע
      **לפי תוכן העמוד בתמלול**, ולא לפי חשבון: בשבעה מהם אומת העמוד והמספר הומר לשיעור
      של העמוד (כ"ט→כ"ה בעמ' 18 · כ"ד→כ"ה בעמ' 17–18 · כ"ה→כ"ו בעמ' 24 · ל'→כ"ז
      בעמ' 38–39); בשלושה שהם **ציטוטי פרק** בלשון שיעור נכתב הסימון המפורש `פרק`
      (כ"ז — עמ' 92–101 · ל' — עמ' 153–157 · כ"ג — עמ' 24–26, שהתגלה באותו סבב);
      שנים שהם **טווחים החולשים על גבול שיעור** (כ"ז — עמ' 49–50 · ל"ד — עמ' 113–114)
      אומתו ולא שונו; ובאחד שהיה חסר מספר (<!-- verify-ignore -->`[שט | עמ' 189]`<!-- verify-ignore-end -->)
      נוסף **מ"א**.
      **לא שונה שום מספר עמוד.** הכלי מדווח כעת: **885 מראי מקום, 991 בדיקות עמוד,
      0 אי־התאמות**. הרשימה ההיסטורית — `docs/SPEC_DINIM_VESATOT.md` §0.3.2; ההכרעה —
      `docs/DECISIONS.md`.
- [ ] חיבור חי מול גוגל (OAuth) — חסום עד להזנת Client ID אמיתי
- [ ] בדיקת הרצה גרפית ב-Electron — לא בוצעה. **נוסף (2026-09-17):** פאנל הוסת הקבוע
      (`#chazaka-container`) והמתג בהגדרות נכתבו ונבדקו ביחידה מול DOM מדומה בלבד — נדרש
      אימות ויזואלי: שלוש ראיות באותו יום ובאותה עונה, ואז לראות שהפאנל מציג את הוסת הקבוע,
      שהלוח מסומן ב`וק"ח` בלבד (בלי `עו"ב`), ושהמתג מחזיר את המצב הקודם.
      **נוסף (17/9, סבב חזרה מן הסילוק):** הפאנל "יציאה מן הסילוק" אומת מול DOM מדומה בלבד.
      המנוע הורץ בהצלחה בדפדפן של האפליקציה (ייבוא `js/calculations.js` ו-`hebcal.js`), והתוצאה
      היתה הבנויה — וסת קבועה שחזרה, מעוגנת בתום ההריון — ולכן נדרש רק סיור ויזואלי:
      למלא מצב חיים (הריון + לידה), ולראות שהסעיף מופיע, שהלוח אינו מסומן בימי הסילוק,
      וששורת "תם הסילוק" מופיעה בטבלת הריכוז.
      **נוסף (17/9, סבב יום ההפסקה בכדורים):** סעיף "חשש יום ההפסקה בכדורים" נבדק מול DOM
      מדומה, והמנוע הורץ בדפדפן האפליקציה (ייבוא `js/calculations.js` ו-`js/pillPause.js`) —
      התקבלו ארבעה ימי חשש בשתי העונות (ימים ב'–ה' שאחרי ההפסקה) וסימון `פה"כ` בלוח.
      נדרש סיור ויזואלי: לרשום שתי תקופות כדורים עם תאריכי הפסקה, ולראות שהסעיף מופיע,
      שכל יום הפסקה מסומן בלוח ב`פה"כ`, ושעם הפעלת מנהג אור זרוע אין `עוא"ז` בימים אלה.
      **נוסף (17/9, סבב וסת הגוף):** סעיף "וסת הגוף — מיחושים שתועדו" והסימון `מיחוש` ביום הראייה
      נבדקו מול DOM מדומה ובבדיקות יחידה בלבד. נדרש סיור ויזואלי: לסמן מיחוש (עם אישור "משונה וקשור")
      על שלוש ראיות באותו יום בחודש, ולראות שהסימון `ומ"ח` מופיע ביום שבחודש הבא, שאין `עוא"ז`
      לפניו, ושהפאנל מציג את וסת המורכב ("נקבע וסת מורכב — יום ... יחד עם פיהוק") — וכן שסימון המיחוש
      נשמר לאחר רענון ובגיבוי/שחזור.
      **נוסף (17/9, סבב סימוני יום):** פאנל "סימוני היום והדינים התלויים בהם" והסימונים על גבי
      היום נבדקו מול DOM מדומה ובבדיקות יחידה בלבד. נדרש סיור ויזואלי: לסמן **יציאה לדרך**
      על יום שעונת אור זרוע חלה בו, ולראות שהעונה ירדה מן הלוח וששורת "`עוא"ז` שנסתלק"
      מופיעה עם הטעם; לסמן **כתם** ולראות שאינו מוסיף חשש; לסמן **פחד פתאום** ולהפעיל את
      המתג — ולראות שנכנסת דרישת בדיקה מתוארכת; ולבדוק שכל הסימונים נשמרים לאחר רענון
      ובגיבוי/שחזור (עמודת "סימוני היום" בגיליון).
- [ ] **החלטה על קבצי המקור ב-git:** שלושת מסמכי האפיון שהועברו ושני ספרי
      המקור (PDF) נמצאים כעת בעקיפין ב-`git status` ולא הוחרגו. הספרים מוגנים
      בזכויות יוצרים (כמו התמלולים שכבר הוחרגו) — החלטה שלך אם להחרגם או לעקוב.
- [ ] **המרת כל הציטוטים לסימון פרק+שיעור:** הספר משתמש בשתי מערכות מספור —
      **פרקים** (כ"ב–ל"ב) ו**שיעורים** (פנימיים). מפֶת הפרקים המלאה (עמודים ותוכן)
      נמצאת ב-`docs/SPEC_DINIM_VESATOT.md` §0.3. כיום הציטוטים באפיון הם לפי
      **שיעור+עמוד** (ומעתה — מאומתים, ראו §0.3.1–0.3.2). **התקדמות (2026-09-17, סבב
      שני):** שלושת הציטוטים שהם **בעצם ציטוטי פרק** סומנו במפורש `פרק` (כ"ז · ל' · כ"ג),
      והם מאומתים כעת בכלי מול **מפת הפרקים** שהתווספה לו. **מה שנותר:** להוסיף לכל ציטוט
      שיעור גם את **הפרק** שבו הוא יושב — כדי שיהיה ניתן לאתרו בספר המודפס ובמסמכי
      האפיון החיצוניים (שמצטטים לפי פרק).

---

## הושלם (משימות מקור)

- [x] **קריאת "דעת טהרה — רק וסתות"** — הספר (16 עמ') סרוק כתמונות, **מסובב 90°** וללא
      שכבת טקסט. נפתר ע"י Tesseract + מודל `heb` וכלי ייעודי (`tools/ocr_scanned_hebrew_pdf.py`)
      שמזהה סיבוב אוטומטית. תוצר: 42,340 תווי עברית, מקומי ב-`מקורות - תמלולים/`.
      פועלו לתוך האפיון ב-`docs/SPEC_DINIM_VESATOT.md` §9.

---

## תוכנית מימוש — הפערים שעלו מהאפיון החדש

על-פי החלטת המשתמש ("הכל"), להלן סדר העבודה. **השלב 1 הוא תנאי לכל השאר** —
זיהוי וסת קבוע משנה את מבנה המנוע, ויתר השלבים נשענים עליו.

> **מצב התוכנית (אומת מול הקוד 2026-09-17): הושלמה כולה.** כל חמשת השלבים (0–4)
> ושלב 5א–5ג בוצעו; אין בתוכנית הזו סעיף אחד שלא מומש. מה שנשאר פתוח בפרויקט אינו
> המשך של תוכנית זו, אלא **החלטות חדשות**: סעיפים שנתגלו מאוחר יותר, שאינם ממומשים
> אלא תלויים בהכרעת בעל האפליקציה או נדחו במכוון — ראו "ממתין" לעיל ו-§10.1 באפיון.

### שלב 0 — שדות קלט חסרים (תשתית, חייב לקום לפני שלב 1)

*(שלושת השדות שוכנו במהלך המימוש, וסימונם כאן עודכן בדיעבד — ר' סבב 3 להלן.)*

- [x] שדה **"ממעיין סתום / ממעיין פתוח"** לכל ראייה (או שדה `durationDays`):
      **תנאי הכרחי** לקביעות — "דוקא כשראתה ב' הראיות הראשונות ממעיין סתום"
      (`[ד"ט | עמ' 7]`). בלי זה אין לקבוע וסת. **מומש (B5).**
- [x] שדה **"ספק עונה"** על הראייה (`[ד"ט | עמ' 1]`). **מומש (B6).**
- [x] מתג **"בדקה כדין בעומק ובחו"ס"** בעונת הוסת (נדרש לעקירה — `[שט מ"א | עמ' 182]`).
      **מומש בלוחית הבדיקה (B4)** — בדיקה בעומק מול קינוח בלבד.

### שלב 1 — מנוע וסתות מבוסס-חזקה (הפער המרכזי)
- [x] זיהוי **וסת החודש קבוע**: 3 ראיות באותו תאריך + **אותה עונה** + ממעיין סתום. *(2026-09-17)*
- [x] זיהוי **וסת ההפלגה קבוע**: 4 ראיות / 3 הפלגות שוות באותה עונה. *(2026-09-17)*
- [x] כשנקבע וסת — לחדול מהצגת שאר החששות ולהציג רק את זמן הוסת הקבוע, **תוך הצגת מה שהוסר**
      ומדוע. *(2026-09-17)*
- [x] **מצב חיים** (מעוברת/מניקה/זקנה/כדורים) — **מומש (2026-09-17):** מבטל חששות שהיו קודם הסילוק,
      מעניק פטור מבדיקה, ומעכב עקירה בכדורים.
- [x] **השלמת כללי העונות המעורבות** (§3.1) — **מומש (2026-09-17):** `mixedOnaChange`
      ב-`js/chazaka.js` וסימון `עו"מ` בחשש שבעונה שכנגד. (הדעה המחמירה שבסעיף א' לעיל — 
      "ג' ביום והרביעית בלילה" — היא שמומשה; דין הצמצום לא מומש במכוון.)

### שלב 2 — עקירה
- [x] עקירת **וסת שאינו קבוע** בעונה שעברה ולא ראתה. *(2026-09-17)*
- [x] עקירת **וסת קבוע**: 3 זמנים רצופים **+ בדיקה כדין**; הפרדה בין "עבר ובדקה" ל"עבר ולא בדקה". *(2026-09-17)*
- [x] עקירת **וסת ההפלגה** לפי `periodsToClear(span) = span*3 - 2` (`[שט מ"א | עמ' 183]`). *(2026-09-17)*
- [x] **עקירת וסת שאינו קבוע — האם נדרשת בדיקה (מתג חומרא, §5ב)** — **מומש כמתג (2026-09-17):**
      `checkUprootNonFixed` ב-`js/stringencies.js`. ברירת המחדל (מתג כבוי) מיושמת ללא בדיקה
      (ברירת המחדל שאושרה); אם יתברר שדרושה בדיקה — הנתון כבר נאסף, נדרש מתג בלבד.
- [x] **בדיקה שנעשתה באיחור — האם היא מועילה גם לאיסור על הבעל** (מחלוקת הב"י והחוות
      דעת `[שט מ"א | עמ' 182–183]`) — **מומש כמתג (2026-09-17):** `lateBedika`
      (דלוק = כהב"י; כיבויו מחייב בדיקה בזמן הוסת עצמו), כמבואר ב-§4.1.

### שלב 3 — וסתות שאינן שכיחות (בזהירות רבה)
- [x] **וסת הדילוג** ו**וסת הסירוג** — **מומש (2026-09-17)** (`js/vesetDilug.js`): הזיהוי מצומצם לתבנית ודאית ומוכחת ממש כפי שנדרש; ראו אזהרת
      הספר: "בוסתות לא שכיחות אין חוששים אלא א"כ הוקבעו באופן ודאי" (`[ד"ט | עמ' 7]`).
      **מומש (2026-09-17)** — ומנוע וסת השבוע (`וק"ש`) יכול לשמש תשתית.
- [x] **וסת השבוע.** — **הושלם ומיושם (2026-09-17):** `js/chazaka.js` (`trailingSameWeekdayRun`)
      + הקרנה ב-`js/calculations.js` בקוד `וק"ש` (כל שבעה ימים) + עקירה ב-`js/akira.js` וחזרה
      מן הסילוק ב-`RETURN_RULES.week`. **הכרעת המימוש:** נדרש גם **הפרש שבוע בדיוק**, ולא זהות
      יום בשבוע בלבד, מחמת אזהרת הספר מפני וסת שבועי מדומה `[ד"ט | עמ' 7]`. ראו `DECISIONS.md`
      ו-`tests/vesetWeek.test.js`.
- [x] **וסת הגוף והווסת המורכב** — **הושלם ומיושם (2026-09-17):** `js/vesetGuf.js` + חיבור ב-`js/calculations.js`.
      מה מומש: (א) חשש **בפעם אחת** ובוודאי **במיחוש משונה וקשור** (`[שט ל"ט | עמ' 158]`, `[ד"ט | עמ' 11]`);
      (ב) ג' פעמים לאותו מיחוש = וסת הגוף קבועה (`[שט ל"ט | עמ' 158]`);
      (ג) **וסת מורכב** — ג' ראיות רצופות של היום והמיחוש (`[שט מ' | עמ' 180]`), סימון היום ב-`ומ"ח`/`ומ"ה`,
      בלי עונת אור זרוע (`[שט כ"ז | עמ' 49]`); (ד) אם ראתה בנתיים ביום הוסת בלא המיחוש — לא נקבעה וסת מורכבת;
      (ה) מיחוש שתועד על ראייה שאינה נספרת (אונס/המשך) מדווח ולא קובע. ראו `docs/DECISIONS.md`.

### שלב 4 — תיקונים נקדותיים (זולים ובטוחים)
- [x] **יו"ח בחודש חסר:** **הושלם ומיושם (2026-09-17)** — נוספה **הדעה השלישית (א' בחודש הבא,
      בתורת ראש חודש)** (`[ד"ט | עמ' 4]`): שלושת התאריכים מסומנים בקוד `יו"ח*` וממוינים.
      ראו `DECISIONS.md` ו-`tests/engine.test.js`.
- [x] **משיכת הראייה:** **הושלם ומיושם (2026-09-17)** — `js/reiyaDuration.js`: החששות נרשמים
      גם בעונות הסמוכות שהדימום נמשך בהן (עד ג' עונות נוספות), ומד' ימים נוספים ומעלה
      אין צריך לחוש אלא לתחילת הראייה (`[ד"ט | עמ' 1]`, שו"ע קפ"ד ה'). ראו `DECISIONS.md`.

---

## פריטים שהועברו במסמכי האפיון החדשים (2026-09-17)

שלושה מסמכים הועברו (`הלכות וסתות.docx`, `אפיון הלכתי-אלגוריתמי_.docx`,
`מפרט הלכתי-אלגוריתמי לחישוב וסתות.pdf`). **הם אפיון טכני — לא מקור**, ומראי המקום
שבהם **אינם תואמים** את מספור הספר. כל פריט שלהלן **אומת מחדש בגוף הספר**. הניתוח
המלא — `docs/SPEC_DINIM_VESATOT.md` §11.

### שלב 5א — כללים מאומתים שנכנסים למודל הקיים (ללא שינוי ארכיטקטוני)

- [x] **וסת הגוף — חומרת בדיקה:** "משעה שבאו המיחושים אסורה כדין שעת הוסת"
      → אסורה **משעה שבא המיחוש**, ואם עבר ולא בדקה — אסורה עד שתבדוק; מוצג בפאנל וסת הגוף.
      **מומש (2026-09-17)** `[שט ל"ט | עמ' 158]`, `[שט ל"ט | עמ' 160]`
- [x] **וסת מורכב (יום + מיחוש):** נקבע בג' ראיות **רצופות**; ראתה בינתיים ביום הוסת
      בלא המיחוש — לא קבעה. `[שט מ' | עמ' 180]` — **מומש (2026-09-17)** (`js/vesetGuf.js`).
      נוסף בה: **החשש ביום עצמו אף קודם שבא המיחוש**, ומנגד **בלי עונת אור זרוע** `[שט כ"ז | עמ' 49]`,
      וסימון `ומ"ח` / `ומ"ה` בלוח.
- [x] **שתי בדיקות בעונת הוסת:** **מומש (2026-09-17):** סימון "בדקתי פעמיים בעונה זו"
      בפעולות היום (יום: עם הקימה וסמוך לשקיעה; לילה: סמוך לשקיעה ולפני השינה) `[שט ל' | עמ' 77]`;
      הסימון נשמר בעמודת אופן הבדיקה בגיליון (`parseCheckKind`), ואינו מתערבב עם עומק הבדיקה.
      **מה במכוון לא נעשה:** רשומת בדיקה שנייה נפרדת — מודל הנתונים הוא רשומה אחת ליום, והדין
      עצמו אינו משתנה (`docs/DECISIONS.md`).
- [x] **מסולקת דמים פטורה מבדיקה** — מעוברת/מיניקה/זקנה/קטנה `[שט כ"ה | עמ' 17–18]` —
      **מומש (2026-09-17):** `exemptFromCheck` במנוע מצב החיים, ומוצג במקום להסתיר בדיקה.
- [x] **כדורים — עיכוב עקירת הוסת** `[ד"ט | עמ' 8]` — **מומש (2026-09-17):** ימי נטילה אינם
      נמנים למניין, וראייה מחמת כדורים אינה מפסיקה אותו.
- [x] **כדורים — חשש יום ההפסקה:** חוששת שתראה בהפסקה הבאה באותו יום; ואף קובעת
      וסת לזה. "רואה מיד אחר שני ימים עד חמשה". `[שט כ"ז | עמ' 40–41]` —
      **מומש (2026-09-17):** `js/pillPause.js` (יום א' מותר, ימים ב'–ה' בחשש, היום שקבעה
      לעצמה) + הזרקה ללוח בסימון `פה"כ` ופאנל במצב חיים. **והושלמה ההבחנה בין סוגי
      הכדורים (2026-09-17):** סוג `orgast` אינו מחשב חלון `[שט כ"ז | עמ' 40]`.
- [~] **מאכל חריף / כדורים = דין וסת הגוף** (חוששת גם בפעם אחת), ולא כוסת האונס.
      `[שט כ"ז | עמ' 41]` — **מומש חלקית:** `sharp`/`pills` נספרים לחזקה כוסת הגוף (ולא
      כוסת האונס), אך אין עדיין מסלול חשש נפרד ל"בפעם אחת" מחמת המאכל עצמו (חששותיו
      של כל וסת נפרד כבר מוצגים ממילא מכל ראיה).
- [x] **פטור מעונת אור זרוע** בוסת מחמת כדורים `[שט כ"ז | עמ' 40]` — **מומש
      (2026-09-17):** החששות של יום ההפסקה, וכל חששותיה של ראייה שסומנה כמחמת כדורים,
      אינם מוסיפים את העונה שלפניה. *(המראה תוקן מ"עמ' 42" ל"עמ' 40" — שם נאמר הדין
      במפורש, ואומת בתמלול.)*
- [x] **חזרה ממצב מסולקת:** לוסת ימים/ראשי חדשים — חוששת **מיד** אחר הסילוק;
      לוסת ההפלגה — רק אחרי שראתה פעם אחת. `[שט כ"ט | עמ' 71]` (שו"ע סל"ד) —
      **מומש (2026-09-17):** `js/silekReturn.js` + פאנל "יציאה מן הסילוק" ב-`js/ui.js`.
- [x] **וסת לימים המתחלפים (ימי המבוכה):** **מומש (2026-09-17)** — `js/chazaka.js` (`findAlternatingDays`), הקרנה לשני ימי החודש וסימון היום; רואה בכ"ז ובכ"ט ולא בכ"ח — חוששת לכ"ז
      ולכ"ט; עקירתו בג"פ. `[שט ל"ב | עמ' 107–108]`
- [x] **תנאי סף בכל חזקה:** ג' הראיות הקובעות באותה עונה — "אפילו שלשה ראשי חדשים
      זה אחר זה". `[שט ל"ד | עמ' 114]` (שו"ע קפ"ט סי"ג) — **מומש ב-2026-09-17**
      (`js/chazaka.js`: `trailingSameDayRun` ו-`trailingEqualSpans` בודקות שהעונות שוות).

### שלב 5ב — מתגי חומרה (מחלוקות שמוצגות למשתמש, לא נעולות בקוד)

- [x] **עקירת וסת שאינו קבוע — האם נדרשת בדיקה:** **מומש כמתג (2026-09-17)** — `checkUprootNonFixed`. המקור מביא את שתי השיטות
      (`[שט ל"א | עמ' 85]` מול `[שט מ"א | עמ' 182]`). ברירת מחדל: ללא בדיקה.
- [x] **ספק עונה:** **מומש כמתג (2026-09-17)** — `safekOnaBoth`: ברירת המחדל העונה המאוחרת בלבד, ולחוש לשתיהן — חומרא במתג.
- [x] **יום ל"א** בעונה בינונית (חומרא קיימת) — **נשאר כמתג** (`orZaruaDay31`).
- [x] **יו"ח בחודש חסר** — **נשאר כפי שהוא (2026-09-17):** שלושת התאריכים מוצגים כברירת מחדל, בלי מתג צמצום — צמצום היה מחייב הכרעה בין הדעות (ראו `DECISIONS.md`).

### שלב 5ג — דורש קלט חדש בממשק (תנאי לשלב 1/2)

- [x] **מצב חיים בהגדרות** (תאריך הריון / לידה / הנקה / גיל / כדורים) — **מומש (2026-09-17):**
      כולל רשימת תקופות נטילת כדורים והודעת פטור מבדיקה במודאל היום.
- [x] **סימון "אונס / קפיצה / מאכל חריף / כדורים"** (ראייה כזו אינה נספרת לחזקה) —
      **מומש (2026-09-17):** שדה "סוג הראייה" בלוחית הראייה (B3).
- [x] **סימון מיחוש גופני** על ראייה — תנאי לוסת הגוף ולוסת המורכב; **מומש (2026-09-17)** (B2):
      רשימת מיחושים ממודול `js/vesetGuf.js`, אישור מפורש שהמיחוש משונה וקשור, סימון `מיחוש` על יום הראייה,
      ועמודה נפרדת בגיבוי (טאב ראשי + היסטוריה).
- [x] **לוחית בדיקות** (טהור / לא בדקה) ומצב **"ממתין לבדיקה"** — תנאי לעקירת קבוע.
      **מומש (B4, 2026-09-17)** — `type: 'check'` עם עונה ועומק.

### תיקון נדרש בכלי החילוץ (מהלקח של רשומה 9)
- [x] **נשמרה שורת הכותרת בכלי החילוץ (2026-09-17):** `load_lines` קיבל פרמטר `headers`
      שאוסף את שורות הכותרת של כל עמוד בנפרד (במקום לדלג עליהן), וה-`main` כותב אותן
      לקובץ צד `output.txt.headers.txt`. הגופנים של הכותרות נכנסים גם הם למניין הגופנים,
      כדי שלא ייפול על גופן שאינו מופיע בגוף. **אימות:** `python -m py_compile` — עובר;
      **הכלי לא הורץ על PDF אמיתי בסבב זה** (אין בו צורך בעבודה השוטפת), וקריאה חוזרת של
      מפת הפרקים אינה נדרשת.

### מה **לא** ימומש אוטומטית (סיכון הלכתי) — ראו §11.6

- הזזת יום הראייה ע"י ניחוש "דם יתר" לפי דפוס קודם (לשאול את המשתמשת במקום לנחש).
- "השהיית כל החישובים" במסולקת דמים — מנוגד למקור בג' חודשי הריון ראשונים
  `[שט כ"ט | עמ' 63]` ובזמננו לשעת ההנקה `[שט כ"ה | עמ' 18]`.
- מודל "מפתח עונות" לחישוב הפלגה — "מנין ההפלגה הוא לפי מניין הימים ולא העונות"
  `[שט ל"ו | עמ' 137]`.
